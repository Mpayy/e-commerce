version: "2"
overrides:
  go:
    overrides:
      - db_type: "pg_catalog.numeric"
        go_type: "float64"
      - db_type: "pg_catalog.timestamp"
        go_type: "time.Time"
      - db_type: "date"
        go_type: "time.Time"
sql:
  - engine: "postgresql"
    queries: "database/query"
    schema: "database/migration"
    gen:
      go:
        package: "sqlc"
        out: "internal/order/repository/sqlc"
        sql_package: "pgx/v5"